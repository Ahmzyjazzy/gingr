/**
 * Gingr global enums
 */

export enum Status {
  SUCCESS = "success",
  FAILED = "failed",
  ACTIVE = "active",
  INACTIVE = "inactive",
  ENABLED = "enabled",
  DISABLED = "disabled",
  PENDING = "pending",
  BLOCKED = "blocked",
  CLOSED = "closed",
  ABANDONED = "abandoned",
}

export enum CurrencyCode {
  NGN = "NGN",
  USD = "USD",
  EUR = "EUR",
  BGP = "BGP",
}
